; Test B: $m appears twice in the report
; Both occurrences should be replaced with the max value

(edge a d 9)
(src a)

(exec 0
  (, (src $s) (edge $s $t $w))
  (O (max (dup $m and $m to $t) $m $w)))

; Expected: (dup 9 and 9 to d)
; Both $m should become 9
