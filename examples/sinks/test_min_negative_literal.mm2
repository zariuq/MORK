; Test D: Literal guard with negative values
; Tests negative integer parsing and literal guard matching

(neg -10)
(neg -5)
(neg 0)

(exec 0
  (, (neg $v))
  (O (min (neg-ok) -10 $v)))

; Expected: (neg-ok)
; Because min is -10 and literal guard is -10
