; Test: MAX sink debugging
; =========================

; Simple numbers to find max
(val 3)
(val 7)
(val 2)
(val 9)
(val 5)

; Test with simple pattern (like count)
(exec 0
  (, (val $n))
  (O (max (result $m) $m (item $n))))

; Expected: (result 9)
